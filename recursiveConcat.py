strings = ['hello', 'world']

def concact(strings):
    x = 0 

    for i in range(len(strings)-1):
        name = strings[i]
        name2 = strings[i+1]
        

    if strings is None: 
        return 
    else: 
        head = strings[:1]
        tail = strings[1:]
        # return head + tail 

print(concact(strings))